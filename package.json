{
  "name": "tokyo-flutter-hackathon-2025-team-PlayGround",
  "private": true,
  "workspaces": [
    "backend/lambda",
    "backend/lambda/generate-ai-recipe",
    "backend/lambda/presigned-url"
  ],
  "scripts": {
    "lambda:package": "npm run package -w @genkaimeshi/generate-ai-recipe -w @genkaimeshi/presigned-url",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "dependencies": {
    "@google/genai": "^1.30.0"
  }
}
