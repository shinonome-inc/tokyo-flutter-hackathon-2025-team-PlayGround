{
  "name": "@genkaimeshi/presigned-url",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "package": "npm run build && mkdir -p ../dist && rm -rf ../dist/presigned_url.zip && cp -r node_modules dist/ && cd dist && zip -r ../../dist/presigned_url.zip . -x '*.map' -x 'presigned_url.zip'",
    "build:package": "npm run package"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.705.0",
    "@aws-sdk/s3-request-presigner": "^3.705.0",
    "@types/aws-lambda": "^8.10.130",
    "uuid": "^11.0.3"
  }
}
