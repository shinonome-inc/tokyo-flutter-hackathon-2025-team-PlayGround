{
  "name": "@genkaimeshi/generate-ai-recipe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "test:local": "npm run build && ts-node test-local.ts",
    "package": "npm run build && mkdir -p ../dist dist/node_modules && rm -rf ../dist/recipe_ai_generator.zip && cp -rL ../../../node_modules/@aws-sdk ../../../node_modules/@google ../../../node_modules/@google-cloud ../../../node_modules/uuid dist/node_modules/ 2>/dev/null || true && cd dist && zip -r ../../dist/recipe_ai_generator.zip index.js node_modules",
    "build:package": "npm run package"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.705.0",
    "@aws-sdk/client-secrets-manager": "^3.939.0",
    "@google-cloud/aiplatform": "^3.32.0",
    "@google/generative-ai": "^0.24.1",
    "@types/aws-lambda": "^8.10.130",
    "uuid": "^11.0.3"
  }
}
