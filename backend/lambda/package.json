{
  "name": "@genkaimeshi/lambda-base",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:package": "npm run build --workspaces",
    "test": "echo \"No tests specified for Lambda base project\""
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "@types/aws-lambda": "^8.10.130",
    "ts-node": "^10.9.2"
  }
}